<%= content_tag :h4, @test_passage.current_question.body, class: 'blog-post-title', dada: { question_index: @test_passage.question_index } %>
  <br>
  <%= form_with url: test_passage_path(@test_passage), local: true, method: :put do |form| %>
    <%= form.collection_check_boxes :answer_ids, @test_passage.current_question.answers, :id, :body, include_hidden: false do |b| %>
      <div class='form-check'>
        <%= b.check_box id: "answer_ids_#{b.object.id}", class: 'form-check-input' %>
        <%= b.label %>
      </div>
    <% end %>
    <br>
    <p><%= link_to t('test_passages.show.gist'), gist_test_passage_path(@test_passage), method: :post %></p>
    <p><%= form.submit t('test_passages.show.next'), class: 'btn btn-sm btn-outline-secondary' %></p>
  <% end %>