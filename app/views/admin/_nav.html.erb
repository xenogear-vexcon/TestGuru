<div class="nav nav-admin">
	<% if user_signed_in? %>
	<span class="nav-admin title"><%= t('.title') %></span>

	<div class="nav-admin user">
		<%= t('.welcome') %>, <b class="nav-admin user-name"><%= current_user.name %></b> Guru |
		<%= link_to t('.sign_out'), destroy_user_session_path, method: :delete %>
	</div>
    <% end %>
</div>